### 本章重点
1. 启动线程
2. 等待线程或分离一个线程
3. 线程唯一标识符
### 主要了解步骤
1. 启动一个线程
2. 等待一个线程结束或者放在后台进行
3. 怎么给启动了的线程来传递参数
4. 怎么将一个线程的所有权从当前的std::thread对象转交给另一个对象
5. 最后在确定线程数,以及识别特定线程
### c++标准库只需要管理std::thread关联的线程(但是比较复杂)

